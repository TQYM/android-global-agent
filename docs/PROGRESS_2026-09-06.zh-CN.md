# 项目进度汇报（交接下一个会话的 AI）

> 日期：2026-09-06 ｜ 主测设备：一加 13T（3B15B401NPF00000，已 root）｜ 副测：荣耀 MagicPad3 Pro（AAQLBB5C03000706，**无 root**，横屏 3200×2136）
> 一句话现状：agent-lite 已按豆包交互规格完成改造并在两台设备实测通过；本轮又修了感知/点击/弹窗三类硬伤，新增规划前置与受限应用纯视觉模式。B 站复杂任务 7 步完成。

---

## 1. 今天干了什么（按时间顺序）

### 1.1 豆包规格全量落地（上午~下午，已完成并实测）
依据《豆包手机助手人机交互模式分析与复刻规格说明书》，在**纯无障碍、零 root** 前提下全量实施，详见 `docs/DOUBAO_SPEC_ADAPTATION.zh-CN.md`：
- `OverlayController`：TYPE_ACCESSIBILITY_OVERLAY 免悬浮窗权限实现灵动岛胶囊 + 粉色呼吸光晕 + 接管/授权/终态卡片
- `AuthStore`（单次/始终/拒绝 × per-app）、`SensitiveGuard`（支付/验证码/协议/权限弹窗强制接管）、`EcoPolicy`（生态三档）、`TaskHistory`（JSONL 归档）、`MemoryStore`（「记一下」记忆+静默注入）
- MainActivity：历史/授权/生态/记忆四个管理页 + 首启说明弹窗

### 1.2 本轮四项修复/增强（16:00–18:30，已构建实测）
| 需求（用户原话） | 实现 | 验证 |
|---|---|---|
| 模型不会联想、不会跳弹窗 | SCHEMA 增「意图联想」「图标联想」（🔍=搜索等）「弹窗处理」三段规则 | B 站搜索任务一次通过 |
| 误触自己的弹窗/只看到助手界面 | `AgentA11yService.collectNodes` 改为 `getWindows()` 合并全部 TYPE_APPLICATION 窗口（按 layer 降序），**排除本包名窗口** | B 站任务全程无误触 |
| 平板点不动搜索栏 | `exec tap#index` 先走 `svc.tapNode()`（ACTION_CLICK 爬可点祖先≤8 层），手势坐标兜底 | 日志出现 `tap#105(直点)` |
| 模型无法发现安装应用 | `installedApps()`（PackageManager 真实清单+缓存）→ 任务起步注入匹配项 + `list_apps` 动作 + exec app 模糊解析兜底 | 待更充分验证 |
| 语义理解前置 | `planTask()`：规划模型把口语转「目标/应用/步骤提示/完成判据/风险点」五行简报，失败容错。用户指定 qwen3.8flash **实测 404 不存在**，默认改为 **qwen3.5-flash**（可在配置页改） | 日志「规划简报已生成（qwen3.5-flash）」 |
| 受限应用绕过 AI 限制 | 生态封锁档语义改为「受限·纯视觉尝试」：授权（scope=vision\|pkg）→ visionForced=true → 丢弃节点、纯截图+px/py 操作、敏感环节强制 ask | 代码就绪，淘宝实测未做 |
| ColorOS「想要打开」弹窗每次强制接管 | `autoConfirmLaunch()`：识别该弹窗后代点「仅本次允许」（用户已事前授权该 app，不重复打断） | 待回归 |

### 1.3 关键实测记录（手机）
- B 站：开 app → 关弹窗 → 直点搜索框 → text 输入 → 搜索 → done「第一个视频标题…播放量213」，**7 步完成** ✓
- 京东：搜索链路本身通了（power bank 已进搜索框），但京东网络层持续报「网络连接已断开」（疑风控）——模型按新规则**点刷新重试 2 次后才如实 fail** ✓（话术「任务失败——网络连接已断开…」）。**用户已要求不再用京东测试**。
- 始终授权持久化：`always|app|com.jingdong.app.mall=true`（root cat 验证）✓；二次任务不再弹授权 ✓
- 敏感守卫：京东首启权限弹窗（「仅本次允许」）稳定触发强制接管卡片 ✓（后查明该弹窗实为 ColorOS 跨应用启动确认 → 已加 autoConfirmLaunch 代点）
- 补充通道：面板输入→队列→注入 ✓，但 qwen3.5-omni-plus 会无视补充 → 注入措辞已强化为「【用户最新补充】优先级高于原任务」

## 2. 当前代码结构（agent-lite/，纯 javac CLI 构建）

```
AgentEngine.java     决策循环（~1200 行）：规划前置/已装应用注入/补充/接管/守卫/生态/授权/归档
AgentA11yService.java 感知+执行：collectNodes(全窗口合并+自过滤) / nodeRefs / tapNode / 手势 / 截图
OverlayController.java 全部覆盖层 UI（胶囊/光晕/面板/四种卡片）
NodeInfo.java        toPrompt(可点击+顶部优先截断40) / fingerprint(全节点加权哈希)
AuthStore/SensitiveGuard/EcoPolicy/TaskHistory/MemoryStore  各司其职
MainActivity.java    主界面 + 4 管理页 + 首启说明；配置面板含 etPlanner（语义规划模型）
```

构建：`cd agent-lite && sh build-mac.sh` → `build/agent-lite.apk`（aapt2→javac --release 11→d8→zipalign→apksigner，debug.keystore 与原版共用）。
**`.gitignore` 已补 `dev.local`**（此前缺失，API key 差点可能误提交）。

## 3. 已知问题 / 下一个会话该做的事

1. **京东不可用**：该机京东持续「网络连接已断开」（疑账号/设备风控），别再用它测。
2. **淘宝等受限应用纯视觉模式未实测**：跑「打开淘宝搜索 XX」验证 visionForced 全链路（预期：弹纯视觉授权卡 → 截图+坐标操作 → 成或如实 fail）。
3. **autoConfirmLaunch 未回归**：下次冷启京东/其他 app 时确认日志出现「检测到本助手引发的跨应用启动确认，代点…」且不再强制接管。
4. **「记一下」记忆链路未端到端实测**（adb 打不了中文；需真机手输「记一下我喜欢坐靠窗」→ 记忆管理页应有条目 → 下个任务系统提示词静默注入）。
5. **补充通道模型服从性**：措辞强化后未复测；若仍被无视，考虑把补充插入为每步 perceive 的固定前缀直到 done。
6. **模型弱项**：qwen3.5-omni-plus 在富节点页（300+）仍可能迷失；toPrompt 截断策略（可点击+顶部优先）已缓解，必要时把 max 从 40 提到 60 或把模型换 qwen3.5-plus 系。
7. **uiautomator dump 在手机上会读到旧文件**：dump 前 `rm -f /data/local/tmp/u.xml`，且不吞 stderr（本次踩过：dump 静默失败，cat 到 com.dsh.agent 的陈旧 dump）。
8. **荣耀平板重装会丢无障碍绑定**：`install -r` 后检查 `settings get secure enabled_accessibility_services`，缺则补写（手机这次也遇到一次）。
9. 工作区**未提交 git**（用户未要求）；大量未跟踪文件（zip/apk/审计报告）提交前先过一遍。

## 4. 快速上手命令

```bash
ADB="$HOME/Library/Android/sdk/platform-tools/adb"
# 手机（主）：3B15B401NPF00000 ｜ 平板（无 root 基准）：AAQLBB5C03000706
cd agent-lite && sh build-mac.sh          # 构建
$ADB -s 3B15B401NPF00000 install -r build/agent-lite.apk
$ADB -s 3B15B401NPF00000 shell "tail -f /sdcard/Android/data/com.dsh.agentlite/files/engine.log"
# 日志:/sdcard/Android/data/com.dsh.agentlite/files/engine.log  归档: 同目录 task_history.jsonl
# 手机输入框 etTask 中心≈(352,2474) 运行按钮≈(1020,2473) 胶囊≈(608,345) 卡片关闭≈(608,2450)
# adb 清输入框：tap 字段 → keyevent 123 → keyevent 67 ×120（必须 ≥120 次，60 不够）
```

## 5. 账户与配置

- DashScope 账户**已充值**（15:16 恢复调用）。key 经 `agent-lite/dev.local` 编译进 APK（dev.local 已入 .gitignore，勿提交）。
- 主模型 qwen3.5-omni-plus（视觉）；语义规划模型 qwen3.5-flash；实测可用：qwen3.5-flash / qwen-flash / qwen-plus；不存在：qwen3.8flash / qwen3-flash。
